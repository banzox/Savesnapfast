export const ADS_CONFIG = {
    // قم بتغيير هذه القيمة إلى false عند تقديم الموقع للمراجعة في أدسنس
    // وعند القبول، قم بإرجاعها إلى true لتشغيل الإعلانات
    // Keep every third-party ad format disabled while the site is under
    // AdSense review. Re-enable only after the site is approved and each
    // placement has been checked against the applicable publisher policies.
    enableAdsterra: false,
};
