# =====================================================
# Cloudflare Pages _redirects
# All redirects use 301 (Permanent) for SEO
# Note: Legacy slug redirects are handled by middleware.ts
# to avoid double-redirect chains
# =====================================================

# Legacy sitemap child. Both XML endpoints now return the complete canonical
# URL set directly, so crawlers never depend on a redirect chain.
